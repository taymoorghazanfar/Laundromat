package com.laundromat.delivery.model.util;

public enum TripStatus {

    REQUESTED, ACCEPTED, STARTED, ARRIVED_SOURCE,
    PICKED_UP, ARRIVED_DESTINATION,
    DELIVERED, COMPLETED, DECLINED, CANCELLED
}
